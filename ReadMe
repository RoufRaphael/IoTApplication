1. Run to Start the containers :
sudo chmod +x ./AQ_Setup/aq_setup.sh -> sudo ./AQ_Setup/aq_setup.sh

2. Run to End the containers
sudo chmod +x ./AQ_teardown/aq_teardown.sh -> sudo ./AQ_teardown/aq_teardown.sh

For (Interference Application Host Original)
Influxdb User = admin
Influxdb Password = adminadmin

Grafana User = admin
Grafana Password = adminadmin

(NO Need to run docker-compose up)
